import React from "react";
import { usePage } from "@inertiajs/react";

export default function GameDetail() {
    const { game } = usePage().props;

    return (
        <div className="container mx-auto p-4">
            {/* <form action={route('create-list')} method="post"></form> */}
            fewfwggwr
        </div>
    );
}